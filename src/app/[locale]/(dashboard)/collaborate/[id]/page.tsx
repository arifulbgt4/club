export default function CollaborateDetailsPage({
  params: { id },
  //   searchParams: { page },
}: {
  params: { id: string };
  searchParams: { page: string };
}) {
  return <div>CollaborateDetailsPage id={id}</div>;
}
